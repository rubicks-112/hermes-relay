package com.hermesandroid.relay.ui.theme

import androidx.compose.ui.unit.dp

object HermesIconSize {
    val xs = 14.dp      // status indicators, inline icons
    val sm = 16.dp      // small buttons, timestamps
    val md = 20.dp      // category rows, chips
    val lg = 24.dp      // standard toolbar icons
    val xl = 32.dp      // mic button, large actions
}
